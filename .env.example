# Supabase (Auth + DB) — free tier is enough to start
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Optional: OpenAI for live Ziki (costs money per token)
# Leave empty to keep free mock replies
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
